{
    "global": { "show_in_menu_bar": false },
    "profiles": [
        {
            "complex_modifications": {
                "rules": [
                    {
                        "description": "Caps Lock → Hyper (ctrl+alt+cmd). Tap does nothing — Caps Lock toggle is disabled entirely. Used by AeroSpace for workspace bindings so plain alt+N still types Nordic-layout characters ($, @, {}, [], etc.).",
                        "manipulators": [
                            {
                                "from": {
                                    "key_code": "caps_lock",
                                    "modifiers": { "optional": ["any"] }
                                },
                                "to": [
                                    {
                                        "key_code": "left_command",
                                        "modifiers": ["left_control", "left_alt"]
                                    }
                                ],
                                "to_if_alone": [
                                    { "key_code": "vk_none" }
                                ],
                                "type": "basic"
                            }
                        ]
                    }
                ]
            },
            "name": "Default profile",
            "selected": true,
            "virtual_hid_keyboard": { "keyboard_type_v2": "iso" }
        }
    ]
}